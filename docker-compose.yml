version: '3'
services:
  mysql:
    extends:
      file: .docker/docker-compose.persistence.yml
      service: mysql
  redis:
    extends:
      file: .docker/docker-compose.persistence.yml
      service: redis
  api:
    build: 
      context: .docker/Dockerfile
      target: debug
    image: openecoe/api:debug
    volumes:
      - ./:/app/api
    ports:
      - 5000:5000
      - 5678:5678
    env_file:
      - ./environments/dev.env
    environment:
      # Data for first run if no organization created
      EMAIL: ecoe@umh.es
      PASSWORD: Ki0uchee
      FIRSTNAME: Open
      SURNAME: ECOE
      ORGANIZATION: UMH
      ALEMBIC_UPGRADE: DO
    depends_on:
      redis:
        condition: service_started
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/status/"]
      interval: 30s
      timeout: 10s
      retries: 3
  worker:
    build: 
      context: .docker/Dockerfile
      target: debug
    image: openecoe/api:debug
    entrypoint: [ "flask", "rq", "worker"]
    volumes:
      - ./:/app/api
    env_file:
      - ./environments/dev.env
    depends_on:
      redis:
        condition: service_started

